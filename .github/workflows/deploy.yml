name: Deploy to Pi
on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: self-hosted  # This tells it to run ON YOUR PI, not in the cloud
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Update App
        run: |
          # The runner is already inside the repo folder on your Pi
          git pull origin main
          # Add restart commands here